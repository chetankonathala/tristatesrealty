---
phase: 01-foundation-design-system
plan: 05
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/ui/filter-pill.tsx
  - src/components/ui/toast-provider.tsx
autonomous: true
requirements: [DS-03]

must_haves:
  truths:
    - "Button renders all 5 variants from UI-SPEC: primary (gold bg), secondary, ghost, outline, destructive"
    - "Primary button has gold background, dark text, 44px height, 4px border-radius, uppercase 700"
    - "Input has 44px height, muted background, accent focus ring"
    - "FilterPill toggles between default and selected (gold) states"
    - "Badge renders featured/new variants with accent-muted background"
    - "Skeleton has shimmer animation at 1500ms"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button with 5 UI-SPEC variants + icon-only"
      contains: "primary"
    - path: "src/components/ui/filter-pill.tsx"
      provides: "Toggleable filter pill component"
      min_lines: 20
    - path: "src/components/ui/input.tsx"
      provides: "Styled input with focus ring"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/app/globals.css"
      via: "CSS variables for primary, secondary, destructive colors"
      pattern: "bg-primary"
---

<objective>
Customize shadcn/ui primitives to match UI-SPEC variants and build the FilterPill custom component.

Purpose: Every interactive element must match the luxury dark aesthetic. Shadcn defaults are overridden to use gold primary, sharp corners, and correct sizing.
Output: Button (5 variants), Badge (4 variants), Input, Select, FilterPill, Skeleton (shimmer), Divider, Toast provider — all matching UI-SPEC.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md

<interfaces>
Design tokens in globals.css (from Plan 03):
- --primary: oklch(0.735 0.115 80)        → gold (#C9A84C)
- --primary-foreground: oklch(0.145 0 0)   → dark text on gold
- --secondary: oklch(0.205 0 0)            → card color
- --muted: oklch(0.245 0 0)               → input backgrounds
- --border: oklch(0.305 0 0)              → borders
- --accent: oklch(0.735 0.115 80)         → gold
- --accent-muted: oklch(0.735 0.115 80 / 0.1) → 10% gold
- --destructive: oklch(0.535 0.22 25)     → red
- --radius: 0.25rem                        → 4px sharp corners
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customize Button with UI-SPEC variants and style Badge, Skeleton, Divider</name>
  <files>src/components/ui/button.tsx, src/components/ui/badge.tsx, src/components/ui/skeleton.tsx, src/components/ui/separator.tsx</files>
  <action>
1. **Button** — Override the shadcn button variants to match UI-SPEC exactly:

Modify `src/components/ui/button.tsx` buttonVariants:

**primary** (default variant):
- `bg-primary text-primary-foreground font-bold uppercase text-base tracking-[0.04em] hover:bg-accent-hover`
- Height: 44px (`h-11`), padding: `px-6 py-3`
- Border-radius: uses --radius (4px) from theme

**secondary**:
- `bg-transparent border border-border text-foreground hover:bg-muted`

**ghost**:
- `bg-transparent text-muted-foreground hover:bg-muted hover:text-foreground`

**outline**:
- `border border-accent text-accent hover:bg-accent hover:text-accent-foreground`

**destructive**:
- `bg-destructive text-destructive-foreground hover:bg-destructive/90`

**icon** size variant:
- `h-11 w-11 p-0` (44px square touch target)

All buttons: `font-bold uppercase text-base tracking-[0.04em]` for text buttons. All have `h-11` default for touch target compliance.

Add focus-visible ring: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`

2. **Badge** — Modify `src/components/ui/badge.tsx` variants:

**default**: `bg-muted text-foreground`
**featured**: `bg-accent-muted text-accent` (gold tint)
**new**: `bg-accent-muted text-accent`
**status**: `bg-muted text-muted-foreground`

All badges: `text-xs uppercase font-bold tracking-[0.05em] h-6 px-2 rounded` (12px Montserrat 700, 24px height, 4px radius)

3. **Skeleton** — Modify `src/components/ui/skeleton.tsx`:
Add shimmer animation. The component should have:
- Background: `bg-muted`
- Shimmer: CSS `@keyframes shimmer` that sweeps a linear-gradient from `--muted` through `--border` back to `--muted` over 1500ms infinite
- Add the shimmer keyframes either inline or in globals.css under `@layer base`

```css
/* Add to globals.css if needed */
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
```

Skeleton component: `bg-muted bg-[length:200%_100%] bg-[linear-gradient(90deg,var(--muted)_25%,var(--border)_50%,var(--muted)_75%)] animate-[shimmer_1.5s_linear_infinite]`

4. **Separator** (Divider) — Verify `src/components/ui/separator.tsx` uses `bg-border`. shadcn default should already be correct. If not, update to `bg-border` for both horizontal and vertical.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Button has 5 variants (primary/secondary/ghost/outline/destructive) matching UI-SPEC. Badge has 4 variants (default/featured/new/status). Skeleton has 1500ms shimmer animation. Separator uses border token. All 44px touch targets on buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Style Input, Select, create FilterPill, and set up Toast provider</name>
  <files>src/components/ui/input.tsx, src/components/ui/select.tsx, src/components/ui/filter-pill.tsx, src/components/ui/toast-provider.tsx</files>
  <action>
1. **Input** — Modify `src/components/ui/input.tsx`:
Per UI-SPEC Input spec:
- Background: `bg-muted`
- Border: `border border-border`
- Focus: `focus:border-accent focus:ring-1 focus:ring-ring` (ring is gold at 50%)
- Height: `h-11` (44px)
- Padding: `px-4 py-3`
- Text: `text-foreground text-base`
- Placeholder: `placeholder:text-muted-foreground`
- Border-radius: `rounded` (uses --radius = 4px)
- Error variant: Add an `error` variant or pass via className — `border-destructive focus:ring-destructive/50`

2. **Select** — Modify `src/components/ui/select.tsx`:
Same styling as Input. Ensure the trigger has:
- `bg-muted border border-border h-11 px-4 text-base text-foreground`
- Chevron icon: should use Lucide `ChevronDown` in `text-muted-foreground`
- Focus: same ring as Input

3. **FilterPill** — Create NEW component `src/components/ui/filter-pill.tsx`:
This is a custom component (not from shadcn). It's a toggleable pill button.

```typescript
"use client";

import { cn } from "@/lib/utils";

interface FilterPillProps {
  selected?: boolean;
  onClick?: () => void;
  children: React.ReactNode;
  className?: string;
}

export function FilterPill({ selected = false, onClick, children, className }: FilterPillProps) {
  return (
    <button
      type="button"
      onClick={onClick}
      className={cn(
        "h-9 px-4 rounded-full text-sm font-medium transition-colors",
        "border focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
        selected
          ? "bg-accent-muted text-accent border-accent"
          : "bg-muted text-foreground border-border hover:border-border-hover",
        className
      )}
    >
      {children}
    </button>
  );
}
```

Per UI-SPEC: Height 36px (`h-9`), border-radius 9999px (`rounded-full`).
Default: muted bg, foreground text, border.
Selected: accent-muted bg, accent text, accent border.

4. **Toast provider** — Create `src/components/ui/toast-provider.tsx`:
Set up Sonner (shadcn's toast solution) with the correct theme:
```typescript
"use client";

import { Toaster } from "sonner";

export function ToastProvider() {
  return (
    <Toaster
      theme="dark"
      position="bottom-right"
      toastOptions={{
        duration: 5000,
        style: {
          background: "var(--card)",
          border: "1px solid var(--border)",
          color: "var(--foreground)",
        },
      }}
    />
  );
}
```

Per UI-SPEC: bottom-right, bg-card, auto-dismiss 5s. Left border accent colors per type will be handled by Sonner's built-in success/error styling.

Add `<ToastProvider />` to `src/app/layout.tsx` inside the ClerkProvider, after `{children}`.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Input and Select styled with 44px height, muted bg, accent focus ring. FilterPill component created with default/selected toggle. ToastProvider configured with dark theme, bottom-right, 5s auto-dismiss. All build clean.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Button has primary/secondary/ghost/outline/destructive variants
- Badge has default/featured/new/status variants
- Skeleton has shimmer animation
- Input has h-11, muted bg, accent focus
- FilterPill exists with toggle behavior
- ToastProvider added to root layout
</verification>

<success_criteria>
- All UI primitives match UI-SPEC dimensions, colors, and interaction states
- 44px minimum touch targets on all interactive elements
- Gold accent used only for primary buttons, featured badges, selected filters, and focus rings
- Skeleton shimmer at 1500ms
- Toast notifications positioned bottom-right with dark theme
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-05-SUMMARY.md`
</output>
