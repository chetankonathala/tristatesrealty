---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/fonts.ts
  - src/lib/utils.ts
  - .env.local
  - .env.example
  - .gitignore
  - next.config.ts
autonomous: true
requirements: [INFRA-01, INFRA-06]

must_haves:
  truths:
    - "Next.js 16 project builds and runs locally on localhost:3000"
    - "TypeScript strict mode enabled with no compilation errors"
    - "Tailwind v4 CSS-first configuration active (no tailwind.config.js)"
    - "All environment variables documented in .env.example"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts and dark class"
      contains: "dark"
    - path: "src/lib/fonts.ts"
      provides: "Playfair Display + Montserrat font declarations"
      exports: ["playfairDisplay", "montserrat"]
    - path: ".env.example"
      provides: "Complete env var inventory for Phase 1"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/fonts.ts"
      via: "import font variables"
      pattern: "import.*fonts"
---

<objective>
Initialize the Next.js 16 project with all Phase 1 dependencies, TypeScript, Tailwind v4, font configuration, and environment variable setup.

Purpose: Create the foundational project that all other Phase 1 plans build upon.
Output: A running Next.js 16 app at localhost:3000 with all dependencies installed and env vars documented.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
1. Run `npx create-next-app@latest tristatesrealty --typescript --tailwind --eslint --app --src-dir` in the parent directory. If the project directory already exists, run from within it or adjust accordingly.

2. Install all Phase 1 dependencies in one batch:
```bash
npm install framer-motion@latest lucide-react @supabase/supabase-js @supabase/ssr @clerk/nextjs mapbox-gl react-map-gl zod react-hook-form @hookform/resolvers
# framer-motion@latest = v12 (UI-SPEC lists v11 but RESEARCH.md confirms v12 — use latest)
npm install -D @types/mapbox-gl supabase
```

3. Install tw-animate-css (NOT tailwindcss-animate — that is deprecated for Tailwind v4):
```bash
npm install tw-animate-css
```

4. Verify NO `tailwind.config.js` or `tailwind.config.ts` exists. If create-next-app generated one, DELETE it. Tailwind v4 uses CSS-first configuration only.

5. Update `next.config.ts` to be minimal — Next.js 16 uses Turbopack by default, no special config needed:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "images.unsplash.com" },
    ],
  },
};

export default nextConfig;
```

6. Ensure `tsconfig.json` has:
- `"strict": true`
- Path alias: `"@/*": ["./src/*"]`

CRITICAL: Do NOT create a `tailwind.config.js` file. Tailwind v4 uses `@theme` in CSS only.
CRITICAL: Use Next.js 16.x (whatever `create-next-app@latest` installs). Do NOT pin to Next.js 15.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js 16 project builds successfully. All dependencies in package.json. No tailwind.config.js exists. TypeScript strict mode enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Configure fonts, root layout, and environment variables</name>
  <files>src/lib/fonts.ts, src/app/layout.tsx, .env.local, .env.example</files>
  <action>
1. Create `src/lib/fonts.ts` with next/font/google declarations:
```typescript
import { Playfair_Display, Montserrat } from "next/font/google";

export const playfairDisplay = Playfair_Display({
  subsets: ["latin"],
  weight: ["700"],
  variable: "--font-playfair-display",
  display: "swap",
});

export const montserrat = Montserrat({
  subsets: ["latin"],
  weight: ["400", "700"],
  variable: "--font-montserrat",
  display: "swap",
});
```

2. Update `src/app/layout.tsx` to be the dark-first root layout:
```typescript
import type { Metadata } from "next";
import { playfairDisplay, montserrat } from "@/lib/fonts";
import "./globals.css";

export const metadata: Metadata = {
  title: "Tri States Realty | Luxury Homes in DE, MD, NJ & PA",
  description: "Find your dream home across Delaware, Maryland, New Jersey, and Pennsylvania. Luxury real estate with virtual tours, market analytics, and seamless offers.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`dark ${playfairDisplay.variable} ${montserrat.variable}`}>
      <body className="bg-background text-foreground font-body antialiased">
        {children}
      </body>
    </html>
  );
}
```

NOTE: ClerkProvider will be added in Plan 02. Do NOT add it here yet.

3. Create `src/lib/utils.ts` with the cn() helper (shadcn/ui dependency):
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
Install clsx and tailwind-merge if not already present: `npm install clsx tailwind-merge`

4. Create `.env.example` with ALL Phase 1 env vars documented:
```bash
# Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Supabase
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ...

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AWS Lambda (Phase 1: config only)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_LAMBDA_FUNCTION_NAME=tristates-background-jobs
```

5. Create `.env.local` as a copy of `.env.example` with placeholder values. User will fill in real values.

6. Ensure `.gitignore` includes `.env.local` and `.env*.local`.

7. Update `src/app/page.tsx` to a simple placeholder that confirms the app works:
```typescript
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="font-display text-5xl font-bold text-accent">
        Tri States Realty
      </h1>
    </main>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build && npm run dev -- --port 3099 &amp; sleep 5 && curl -s http://localhost:3099 | grep -o "Tri States Realty" && kill %1 2>/dev/null</automated>
  </verify>
  <done>Root layout has dark class + both font variables applied. Fonts load via next/font/google with swap display. .env.example documents all Phase 1 env vars. Dev server renders placeholder page with Playfair Display heading.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with 0 errors
- `npm run dev` starts on localhost:3000
- No `tailwind.config.js` or `tailwind.config.ts` exists
- `grep -r "tailwind.config" . --include="*.ts" --include="*.js"` returns nothing
- `.env.example` exists with all Phase 1 env vars documented
- `src/lib/fonts.ts` exports playfairDisplay and montserrat
</verification>

<success_criteria>
- Next.js 16 project builds and serves locally
- All Phase 1 npm dependencies installed
- TypeScript strict mode with zero errors on build
- Font variables applied to html element
- Environment variable template complete
- Git repository initialized with .gitignore covering .env.local
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
