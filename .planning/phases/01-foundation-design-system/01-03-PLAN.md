---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/globals.css
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/select.tsx
  - src/components/ui/sonner.tsx
autonomous: true
requirements: [DS-01, DS-02]

must_haves:
  truths:
    - "Tailwind v4 CSS-first config active with OKLCH design tokens from UI-SPEC"
    - "bg-background renders #0A0A0A, text-accent renders #C9A84C, bg-card renders #141414"
    - "shadcn/ui components installed and rendering with custom dark theme"
    - "Font utility classes font-display and font-body map to Playfair Display and Montserrat"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete design token system with OKLCH colors + @theme inline mapping"
      contains: "@theme inline"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/ui/*"
      via: "CSS custom properties consumed by shadcn components"
      pattern: "var(--"
---

<objective>
Configure the complete luxury dark design token system in Tailwind v4 CSS-first format and initialize shadcn/ui with all base primitives.

Purpose: Establish the visual foundation so all component plans can use semantic color/spacing/typography tokens consistently.
Output: globals.css with full OKLCH token system, shadcn/ui initialized with 11 base primitives.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete Tailwind v4 design tokens in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Replace the entire contents of `src/app/globals.css` with the complete design token system. This MUST follow the UI-SPEC exactly and use the OKLCH values from 01-RESEARCH.md.

```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

:root {
  /* Core palette — from UI-SPEC, OKLCH format for shadcn/ui compatibility */
  --background: oklch(0.145 0 0);              /* #0A0A0A — 60% usage */
  --foreground: oklch(0.965 0.005 85);          /* #F5F5F0 */
  --card: oklch(0.205 0 0);                     /* #141414 — 30% usage */
  --card-foreground: oklch(0.965 0.005 85);
  --muted: oklch(0.245 0 0);                    /* #1A1A1A */
  --muted-foreground: oklch(0.665 0 0);         /* #A3A3A3 */
  --accent: oklch(0.735 0.115 80);              /* #C9A84C — 10% usage */
  --accent-foreground: oklch(0.145 0 0);        /* Dark text on gold */
  --accent-hover: oklch(0.775 0.115 80);        /* #D4B85E */
  --accent-muted: oklch(0.735 0.115 80 / 0.1);  /* 10% gold */
  --border: oklch(0.305 0 0);                   /* #262626 */
  --border-hover: oklch(0.365 0 0);             /* #333333 */
  --destructive: oklch(0.535 0.22 25);          /* #DC2626 */
  --destructive-foreground: oklch(0.965 0.005 85);
  --success: oklch(0.545 0.165 150);            /* #16A34A */
  --ring: oklch(0.735 0.115 80 / 0.5);         /* Gold at 50% for focus */
  --input: oklch(0.305 0 0);
  --primary: oklch(0.735 0.115 80);             /* Gold = primary action */
  --primary-foreground: oklch(0.145 0 0);
  --secondary: oklch(0.205 0 0);
  --secondary-foreground: oklch(0.965 0.005 85);
  --popover: oklch(0.205 0 0);
  --popover-foreground: oklch(0.965 0.005 85);
  --sidebar-background: oklch(0.205 0 0);
  --sidebar-foreground: oklch(0.965 0.005 85);
  --sidebar-primary: oklch(0.735 0.115 80);
  --sidebar-primary-foreground: oklch(0.145 0 0);
  --sidebar-accent: oklch(0.245 0 0);
  --sidebar-accent-foreground: oklch(0.965 0.005 85);
  --sidebar-border: oklch(0.305 0 0);
  --sidebar-ring: oklch(0.735 0.115 80 / 0.5);
  --radius: 0.25rem;                            /* 4px — sharp luxury feel per UI-SPEC */
}

/* Map CSS vars to Tailwind utilities via @theme inline */
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent-hover: var(--accent-hover);
  --color-accent-muted: var(--accent-muted);
  --color-border: var(--border);
  --color-border-hover: var(--border-hover);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-success: var(--success);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-sidebar-background: var(--sidebar-background);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --radius-sm: calc(var(--radius) - 2px);
  --radius-md: var(--radius);
  --radius-lg: calc(var(--radius) + 4px);
  --radius-xl: calc(var(--radius) + 8px);
  /* Fonts — variables set by next/font in layout.tsx */
  --font-display: var(--font-playfair-display);
  --font-body: var(--font-montserrat);
}

/* Base styles */
@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

CRITICAL: Do NOT use hex values directly. All colors MUST be OKLCH for shadcn/ui opacity modifier compatibility.
CRITICAL: Do NOT create a tailwind.config.js. This CSS file IS the configuration.
CRITICAL: The `--font-display` and `--font-body` variables connect to the CSS variables set by next/font in layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>globals.css contains complete OKLCH design token system matching UI-SPEC. @theme inline maps all tokens to Tailwind utilities. No tailwind.config.js exists.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and install all base primitives</name>
  <files>components.json, src/components/ui/*.tsx</files>
  <action>
1. Run shadcn/ui init:
```bash
npx shadcn@latest init
```
When prompted:
- Style: **Default**
- Base color: **Neutral** (will be overridden by our custom tokens already in globals.css)
- CSS variables: **Yes**

If shadcn init overwrites globals.css, immediately restore the design tokens from Task 1. The shadcn init may add its own CSS variables — merge carefully, keeping OUR OKLCH values as the source of truth.

2. Install all base shadcn/ui primitives needed for Phase 1 components:
```bash
npx shadcn@latest add button input select dialog sheet badge skeleton toast dropdown-menu separator tooltip
```

3. After installation, verify each component file exists in `src/components/ui/`:
- button.tsx
- input.tsx
- select.tsx
- dialog.tsx
- sheet.tsx
- badge.tsx
- skeleton.tsx
- toast or sonner.tsx (shadcn uses Sonner for toasts)
- dropdown-menu.tsx
- separator.tsx
- tooltip.tsx

4. Verify that the shadcn components reference our CSS variable tokens (e.g., `bg-primary`, `text-primary-foreground`, `border-border`) which will resolve to our OKLCH values.

5. If shadcn/ui toast component installs as Sonner, install the sonner package if not already present:
```bash
npm install sonner
```

6. After everything is installed, run `npm run build` to confirm no type errors or build failures.

NOTE: Do NOT customize the shadcn component code yet. Custom variants (like 5 button variants per UI-SPEC) will be built in Plan 05.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && ls src/components/ui/ && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>shadcn/ui initialized with components.json. All 11 base primitives installed in src/components/ui/. Design tokens in globals.css preserved. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `src/app/globals.css` contains `@theme inline` block with all OKLCH tokens
- `components.json` exists in project root
- All 11 shadcn components exist in `src/components/ui/`
- `grep "tailwind.config" -r . --include="*.ts" --include="*.js" --include="*.mjs"` returns nothing (no JS config)
- Font utility classes `font-display` and `font-body` resolve correctly
</verification>

<success_criteria>
- Complete OKLCH design token system in globals.css matching UI-SPEC exactly
- Tailwind v4 @theme inline maps all tokens to utility classes
- shadcn/ui initialized with 11 base primitives
- `bg-background` renders #0A0A0A, `text-accent` renders #C9A84C, `bg-card` renders #141414
- `font-display` maps to Playfair Display, `font-body` maps to Montserrat
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
