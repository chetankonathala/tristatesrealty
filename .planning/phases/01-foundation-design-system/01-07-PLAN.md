---
phase: 01-foundation-design-system
plan: 07
type: execute
wave: 4
depends_on: [01-04, 01-05, 01-06]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/sections/hero.tsx
  - src/components/sections/featured-listings.tsx
  - src/components/sections/communities-section.tsx
  - src/components/sections/cta-banner.tsx
  - public/images/hero-placeholder.webp
autonomous: false
requirements: [DS-04, DS-05]

must_haves:
  truths:
    - "Homepage renders hero, featured listings grid, communities section, CTA banner, footer in correct order"
    - "Hero section fills viewport with background image, overlay gradient, staggered heading + subheading + CTA"
    - "Featured listings section shows 3-column grid of ListingCards with placeholder data"
    - "Communities section shows CommunityCards with placeholder data"
    - "All sections animate on scroll with FadeIn/StaggerChildren"
    - "Page uses semantic HTML: nav, main, footer, section elements"
    - "Fonts load without visible CLS (Playfair Display + Montserrat)"
    - "next build completes with no page bundle exceeding 500KB first load JS (DS-05 pre-deploy proxy)"
    - "No CSS animations use width/height/top/left properties that trigger layout shift (transform/opacity only)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage assembling all sections"
      min_lines: 30
    - path: "src/components/sections/hero.tsx"
      provides: "Full-viewport hero with staggered entrance animation"
      min_lines: 40
    - path: "src/components/sections/featured-listings.tsx"
      provides: "3-column grid of placeholder ListingCards"
      min_lines: 25
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/navbar.tsx"
      via: "imports Navbar as top-level layout"
      pattern: "import.*Navbar"
    - from: "src/app/page.tsx"
      to: "src/components/sections/hero.tsx"
      via: "renders HeroSection as first content"
      pattern: "HeroSection"
    - from: "src/components/sections/featured-listings.tsx"
      to: "src/components/cards/listing-card.tsx"
      via: "renders ListingCard with placeholder data"
      pattern: "ListingCard"
---

<objective>
Assemble the homepage shell with hero section, featured listings, communities preview, CTA banner, and all animations — creating the complete luxury dark landing page.

Purpose: This is the first visual deliverable. The homepage must showcase the design system in action and hit Lighthouse > 90.
Output: Complete homepage at / with all sections from UI-SPEC, scroll animations, correct fonts, responsive layout.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md

<interfaces>
From src/components/layout/ (Plan 04):
- Navbar: sticky, scroll transition
- Footer: 4-column grid

From src/components/cards/ (Plan 06):
- ListingCard: { image, price, address, beds, baths, sqft, featured?, isNew? }
- CommunityCard: { image, name, location, priceRange }

From src/components/motion/ (Plan 06):
- FadeIn: scroll-triggered fade + translate-y
- StaggerChildren: stagger container for children
- PageTransition: route transition wrapper

From src/components/ui/ (Plan 05):
- Button: primary (gold), secondary, ghost variants

UI-SPEC Homepage Shell Layout:
1. Navbar (sticky, transparent -> solid)
2. Hero (full viewport, bg image, gradient overlay, staggered text + CTA)
3. Featured Listings Section (3xl top padding, 3-column grid)
4. Communities Section (3xl top padding, card grid)
5. CTA Banner (full bleed, accent-muted background)
6. Footer

UI-SPEC Copywriting:
- Hero CTA: "Explore Properties"
- Hero secondary: "Get a Home Valuation"
- Footer tagline: "Your Trusted Real Estate Partner in Delaware, Maryland, New Jersey & Pennsylvania"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero section and homepage section components</name>
  <files>src/components/sections/hero.tsx, src/components/sections/featured-listings.tsx, src/components/sections/communities-section.tsx, src/components/sections/cta-banner.tsx</files>
  <action>
1. **HeroSection** — Create `src/components/sections/hero.tsx` as "use client":
Per UI-SPEC hero spec + animation contract:

- Full viewport height: `min-h-screen` with `relative overflow-hidden`
- Background: Use a placeholder dark gradient for now (no real hero image yet). Use `bg-gradient-to-b from-card/50 to-background` as base, or a dark Unsplash image via next/image with priority loading
- Gradient overlay on image: `absolute inset-0 bg-gradient-to-b from-background/40 via-background/60 to-background`
- Content: centered, max-width 560px per UI-SPEC
- Staggered entrance animation (per UI-SPEC animation contract):
  - Heading (0ms delay): "Find Your Dream Home" — font-display, 48px desktop (text-5xl md:text-6xl), 32px mobile, weight 700, text-foreground
  - Subheading (150ms delay): "Discover luxury properties across Delaware, Maryland, New Jersey, and Pennsylvania." — font-body, text-base, text-muted-foreground, max-w-lg
  - CTA buttons (300ms delay): Primary "Explore Properties" + Secondary "Get a Home Valuation"
- Each element wrapped in motion.div with fade + translate-y 16px, 600ms ease-out, respective delays
- Content vertically centered using flexbox

2. **FeaturedListingsSection** — Create `src/components/sections/featured-listings.tsx`:
- Server Component (no "use client" needed for the section, FadeIn/StaggerChildren are client)
- Section padding: `pt-16 md:pt-[64px]` (3xl = 64px per UI-SPEC spacing)
- Content: `max-w-[1280px] mx-auto px-4 md:px-6 lg:px-8`
- Heading: "Featured Properties" — wrapped in FadeIn, font-display text-[28px] md:text-[28px] font-bold
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8`
- Populate with 6 placeholder ListingCards (hardcoded data):
  - Use realistic-looking data: varied prices ($350k-$850k), different bed/bath counts, real-sounding addresses in DE/MD/NJ/PA
  - Use Unsplash placeholder images for houses (or empty div with bg-muted)
  - Mark 1-2 as featured, 1 as new
- Wrap grid in StaggerChildren

3. **CommunitiesSection** — Create `src/components/sections/communities-section.tsx`:
- Same layout pattern as FeaturedListings
- Heading: "Explore Communities"
- Grid: 3 placeholder CommunityCards with Schell Brothers-sounding community names
  - e.g., "Bayberry at Bethany", "The Overlook at Rockhill", "Coastal Club"
  - Price ranges like "$450k - $750k"
- Wrap in FadeIn/StaggerChildren

4. **CTABanner** — Create `src/components/sections/cta-banner.tsx`:
- Full bleed: no max-width constraint on outer wrapper
- Background: `bg-accent-muted` (10% gold tint)
- Inner content: `max-w-[1280px] mx-auto px-4 md:px-6 lg:px-8 py-16 text-center`
- Heading: "Ready to Find Your Dream Home?" — font-display, heading size
- Body: "Let us help you navigate the market with expert guidance and cutting-edge tools." — font-body, text-muted-foreground
- CTA: Primary button "Contact Agent"
- Wrap inner elements in FadeIn
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 section components created with correct typography, spacing, and animation wrappers. Hero has staggered entrance. Listings and Communities show placeholder cards. CTA banner uses accent-muted background. All build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble homepage and wire layout with PageTransition</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <action>
1. Update `src/app/page.tsx` to be the complete homepage shell:
```typescript
import { Navbar } from "@/components/layout/navbar";
import { Footer } from "@/components/layout/footer";
import { HeroSection } from "@/components/sections/hero";
import { FeaturedListingsSection } from "@/components/sections/featured-listings";
import { CommunitiesSection } from "@/components/sections/communities-section";
import { CTABanner } from "@/components/sections/cta-banner";

export default function HomePage() {
  return (
    <>
      <Navbar />
      <main id="main-content">
        <HeroSection />
        <FeaturedListingsSection />
        <CommunitiesSection />
        <CTABanner />
      </main>
      <Footer />
    </>
  );
}
```

2. Update `src/app/layout.tsx` to include PageTransition wrapper around `{children}`:
- Add the PageTransition component inside ClerkProvider, wrapping children
- Ensure the `id="main-content"` target exists on the main element (for skip link from Plan 04)

3. Verify semantic HTML structure:
- `<nav>` for Navbar
- `<main id="main-content">` for content
- `<section>` for each content section (hero, listings, communities, CTA)
- `<footer>` for Footer
- H1 in hero ("Find Your Dream Home"), H2 for section headings

4. Performance considerations:
- HeroSection is "use client" (Framer Motion) but listings/communities sections should be Server Components with client FadeIn/StaggerChildren wrappers
- No unnecessary "use client" on page.tsx itself
- Use `next/image` with `priority` for hero background image
- Lazy load below-fold content (browser native lazy loading via next/image defaults)

5. Run a local dev build and verify the page renders all sections in order.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Homepage renders all sections in correct order: Navbar, Hero, Featured Listings, Communities, CTA Banner, Footer. Semantic HTML with nav/main/footer/section. PageTransition wraps route content. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete homepage shell with luxury dark design: hero section with staggered animations, 3-column featured listings grid with ListingCards, communities section with CommunityCards, CTA banner, responsive Navbar with scroll transition, and Footer.</what-built>
  <how-to-verify>
    1. Run `npm run dev` — visit http://localhost:3000
    2. Verify hero section fills viewport with heading, subheading, and gold CTA button
    3. Scroll down — verify Navbar transitions from transparent to solid dark background
    4. Verify Featured Properties section has 3-column grid of listing cards (placeholder data)
    5. Verify gold accent appears ONLY on: prices, primary CTA buttons, featured badges
    6. Verify Communities section has cards with gradient overlays
    7. Verify CTA banner has subtle gold-tinted background
    8. Resize to mobile — verify responsive: single column cards, hamburger menu appears
    9. Click hamburger — verify mobile menu slides from right
    10. Check fonts: headings are serif (Playfair Display), body is sans-serif (Montserrat)
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- Homepage at / renders all 6 sections (nav, hero, listings, communities, CTA, footer)
- Scroll animations trigger correctly (once only)
- Hero staggered entrance works (heading -> subheading -> CTA)
- Responsive: 3 columns desktop, 2 tablet, 1 mobile
- Semantic HTML validates (nav, main, footer, section, h1-h2 hierarchy)
- No console errors in dev mode
</verification>

<success_criteria>
- Complete homepage matching UI-SPEC layout specification
- All sections visible with correct spacing (3xl = 64px between sections)
- Hero: full viewport, gradient overlay, staggered entrance animation
- Featured: 3-column grid with 6 placeholder ListingCards
- Communities: 3-column grid with 3 placeholder CommunityCards
- CTA: full-bleed accent-muted background
- Navbar: transparent -> solid on scroll
- Footer: 4 columns with brand/links/contact/social
- Responsive at all breakpoints
- Semantic HTML structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-07-SUMMARY.md`
</output>
