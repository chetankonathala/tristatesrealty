---
phase: 01-foundation-design-system
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/components/layout/navbar.tsx
  - src/components/layout/mobile-menu.tsx
  - src/components/layout/nav-link.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/skip-link.tsx
autonomous: true
requirements: [DS-03]

must_haves:
  truths:
    - "Navbar is sticky with transparent-to-solid background transition on scroll past 80px"
    - "Mobile menu slides in from right on hamburger click"
    - "Footer renders 4-column grid on desktop, stacked on mobile"
    - "Active nav link has gold underline indicator"
    - "Skip-to-content link visible on focus"
  artifacts:
    - path: "src/components/layout/navbar.tsx"
      provides: "Responsive sticky navbar with scroll background transition"
      min_lines: 40
    - path: "src/components/layout/mobile-menu.tsx"
      provides: "Full-height slide-over mobile nav"
      min_lines: 30
    - path: "src/components/layout/footer.tsx"
      provides: "4-column responsive footer with brand, links, contact, legal"
      min_lines: 40
  key_links:
    - from: "src/components/layout/navbar.tsx"
      to: "src/components/layout/mobile-menu.tsx"
      via: "state toggle for open/close"
      pattern: "useState.*menuOpen"
    - from: "src/components/layout/navbar.tsx"
      to: "src/components/layout/nav-link.tsx"
      via: "renders NavLink for each route"
      pattern: "NavLink"
---

<objective>
Build the Navbar (with scroll transition + mobile menu) and Footer layout components per UI-SPEC specifications.

Purpose: These layout components wrap every page and must be pixel-perfect to the luxury dark aesthetic before the homepage shell is assembled.
Output: Navbar with transparent-to-solid scroll behavior, mobile hamburger menu, Footer with 4-column layout.
</objective>

<execution_context>
@/Users/chetankonathala/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chetankonathala/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md

<interfaces>
From src/components/ui/button.tsx (shadcn):
- Button component with variant prop (default, destructive, outline, secondary, ghost, link)
- buttonVariants for extending

From src/components/ui/sheet.tsx (shadcn):
- Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription

From src/lib/fonts.ts:
- playfairDisplay (variable: --font-playfair-display)
- montserrat (variable: --font-montserrat)

Design tokens available via Tailwind:
- bg-background, bg-card, text-foreground, text-muted-foreground
- text-accent (gold), border-border, border-border-hover
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Navbar with scroll transition and NavLink component</name>
  <files>src/components/layout/navbar.tsx, src/components/layout/nav-link.tsx, src/components/layout/skip-link.tsx</files>
  <action>
1. Create `src/components/layout/skip-link.tsx`:
Simple accessible skip link — visible only on focus. Positioned above nav. Links to `#main-content`.
```
"Skip to main content" — sr-only by default, visible on focus-visible. Positioned fixed top-2 left-2 z-[100]. Background accent, text accent-foreground, padding sm md.
```

2. Create `src/components/layout/nav-link.tsx` — a "use client" component:
Per UI-SPEC NavLink spec:
- Montserrat 16px weight 700 uppercase, letter-spacing 0.04em
- Active state: gold underline 2px below text
- Hover state: gold underline fade-in 200ms
- Use `usePathname()` from `next/navigation` to determine active state
- Render as Next.js `Link` component

Props: `href: string`, `children: React.ReactNode`, `onClick?: () => void` (for closing mobile menu)

3. Create `src/components/layout/navbar.tsx` — a "use client" component:
Per UI-SPEC Navbar spec:
- Height: 72px desktop, 64px mobile (use h-16 md:h-[72px])
- Position: sticky top-0 z-50
- Background: transparent at page top, `bg-card/95 backdrop-blur-md` after scrolling 80px
- Use `useEffect` + scroll listener to track scroll position. Add/remove classes based on `scrollY > 80`.
- Layout: Logo left (text "Tri States Realty" in font-display text-accent), nav links center (hidden on mobile), CTA button right ("Contact Agent" — use shadcn Button ghost variant for now)
- Mobile: hamburger icon button (Lucide `Menu` icon) replaces center nav and triggers MobileMenu
- Nav links for Phase 1: "Properties", "Communities", "About", "Contact" (all link to "/" for now — placeholder)
- Include SkipLink as first child

CRITICAL performance: Use `will-change-transform` on the nav for smooth background transitions. Do NOT animate height or layout properties.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Navbar renders with sticky positioning, transparent-to-solid background on scroll past 80px. NavLink shows gold underline on active route. Skip link visible on focus. Hamburger icon visible on mobile breakpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build MobileMenu and Footer components</name>
  <files>src/components/layout/mobile-menu.tsx, src/components/layout/footer.tsx</files>
  <action>
1. Create `src/components/layout/mobile-menu.tsx` — a "use client" component:
Per UI-SPEC MobileMenu spec:
- Use shadcn Sheet component (SheetContent from right side)
- Full-height slide-over from right
- Background: `bg-background/[0.98] backdrop-blur-md`
- Close on overlay click or X button (Sheet handles this)
- Render same NavLink items as desktop nav, but stacked vertically with spacing
- Include "Contact Agent" CTA button at bottom
- Animation: Sheet handles the slide animation (250ms)

Props: `open: boolean`, `onOpenChange: (open: boolean) => void`

Wire into Navbar: Navbar manages `open` state, passes to MobileMenu.

2. Create `src/components/layout/footer.tsx` — Server Component (no "use client" needed):
Per UI-SPEC Footer spec:
- Background: `bg-card`
- Top border: `border-t border-border`
- Minimum height: 240px
- Layout: 4-column grid on desktop (`grid-cols-4`), stacked on mobile (`grid-cols-1`)
- Column 1: Logo ("Tri States Realty" in font-display text-accent) + tagline from UI-SPEC copywriting contract: "Your Trusted Real Estate Partner in Delaware, Maryland, New Jersey & Pennsylvania"
- Column 2: Quick Links — Properties, Communities, About, Contact (use standard `Link` tags)
- Column 3: Contact Info — placeholder phone, email, address
- Column 4: Social icons (Lucide: Facebook, Instagram, Twitter/X) — placeholder href="#"
- Bottom bar: "(C) 2026 Tri States Realty. All rights reserved." in text-muted-foreground text-xs
- All text follows typography scale: links in body size (16px), legal in label size (12px)
- Padding: `py-12 px-4 md:px-6 lg:px-8`, content max-w-[1280px] mx-auto

3. Create an index barrel file `src/components/layout/index.ts` exporting all layout components for clean imports.
  </action>
  <verify>
    <automated>cd /Users/chetankonathala/tristatesrealty && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>MobileMenu slides in from right using Sheet. Footer renders 4-column grid on desktop, stacked on mobile. Footer includes brand, links, contact, social, legal copy. All text uses correct typography tokens.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- All layout components exist in `src/components/layout/`
- Navbar uses scroll listener for background transition
- MobileMenu uses shadcn Sheet
- Footer has 4-column responsive grid
- NavLink uses usePathname for active detection
- SkipLink exists and targets #main-content
</verification>

<success_criteria>
- Navbar: sticky, 72px desktop / 64px mobile, transparent -> card background on scroll past 80px
- NavLink: Montserrat 700 uppercase, gold underline on active, hover fade-in 200ms
- MobileMenu: full-height Sheet from right, contains all nav links + CTA
- Footer: 4-column grid desktop, stacked mobile, bg-card, min-height 240px, correct copy
- Skip link accessible and functional
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-04-SUMMARY.md`
</output>
