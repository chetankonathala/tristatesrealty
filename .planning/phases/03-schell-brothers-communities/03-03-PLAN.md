---
phase: 03-schell-brothers-communities
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/communities/community-hero.tsx
  - src/components/communities/community-overview.tsx
  - src/components/communities/community-amenities.tsx
  - src/components/communities/community-schools.tsx
  - src/components/communities/community-hoa.tsx
  - src/components/communities/community-floor-plans.tsx
autonomous: true
requirements:
  - SCHELL-02
must_haves:
  truths:
    - "CommunityHero renders video or image hero with gradient overlay, name, price, and CTA"
    - "CommunityOverview renders description with read-more truncation"
    - "CommunityAmenities renders a grid of amenity chips with mapped Lucide icons"
    - "CommunitySchools renders school cards with district name"
    - "CommunityHoa renders HOA info card or hides when no HOA data"
    - "CommunityFloorPlans renders floor plan cards with image, name, price, specs"
  artifacts:
    - path: "src/components/communities/community-hero.tsx"
      provides: "Full-bleed hero with video/image + CTA"
      contains: "CommunityHero"
    - path: "src/components/communities/community-overview.tsx"
      provides: "Description section with read-more"
      contains: "CommunityOverview"
    - path: "src/components/communities/community-amenities.tsx"
      provides: "Amenities grid with icons"
      contains: "CommunityAmenities"
    - path: "src/components/communities/community-schools.tsx"
      provides: "School info cards"
      contains: "CommunitySchools"
    - path: "src/components/communities/community-hoa.tsx"
      provides: "HOA info display"
      contains: "CommunityHoa"
    - path: "src/components/communities/community-floor-plans.tsx"
      provides: "Floor plan card grid"
      contains: "CommunityFloorPlans"
  key_links:
    - from: "src/components/communities/community-hero.tsx"
      to: "src/types/community.ts"
      via: "Community type import"
      pattern: "import.*Community.*from"
    - from: "src/components/communities/community-floor-plans.tsx"
      to: "src/types/listing.ts"
      via: "Listing type for floor plan data"
      pattern: "import.*Listing.*from"
---

<objective>
Build the six core community detail page section components: hero, overview, amenities, schools, HOA, and floor plans.

Purpose: These are the visual building blocks composing the community detail page. Each renders a distinct section with data from the Community type.
Output: Six client/server components in src/components/communities/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-schell-brothers-communities/03-RESEARCH.md
@.planning/phases/03-schell-brothers-communities/03-UI-SPEC.md
@.planning/phases/03-schell-brothers-communities/03-01-SUMMARY.md

<interfaces>
From src/types/community.ts (created in Plan 01):
```typescript
export interface Community {
  id: number;
  community_id: string;
  slug: string;
  name: string;
  full_name: string | null;
  description: string | null;
  short_description: string | null;
  city: string | null;
  state: string | null;
  zip: string | null;
  address: string | null;
  lat: number | null;
  lng: number | null;
  price_from: number | null;
  school_district: string | null;
  school_elementary: string | null;
  school_middle: string | null;
  school_high: string | null;
  hoa_fee: number | null;
  hoa_yearly_fee: number | null;
  hoa_name: string | null;
  amenities: { name: string; icon?: string }[];
  division_id: string | null;
  division_name: string | null;
  featured_image_url: string | null;
  is_sold_out: boolean;
  is_active: boolean;
  heartbeat_page_url: string | null;
  youtube_video_ids: string[];
  custom_video_urls: string[];
  seo_title: string | null;
  seo_description: string | null;
  sales_center_address: string | null;
  synced_at: string;
  created_at: string;
  updated_at: string;
}
```

From src/types/listing.ts:
```typescript
export interface Listing {
  id: number; mls_id: number; list_price: number; status: ListingStatus;
  address_full: string; address_city: string; address_state: string;
  bedrooms: number | null; bathrooms: number | null; area: number | null;
  lat: number | null; lng: number | null; ...
}
```

From src/components/motion/ (existing):
```typescript
// FadeIn and StaggerChildren wrappers available
import { FadeIn } from "@/components/motion/fade-in";
import { StaggerChildren } from "@/components/motion/stagger-children";
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/components/ui/button.tsx:
```typescript
export function Button({ variant, size, className, ... }): JSX.Element;
// variants: "default" | "outline" | "ghost" | "destructive"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommunityHero + CommunityOverview + CommunityAmenities</name>
  <files>src/components/communities/community-hero.tsx, src/components/communities/community-overview.tsx, src/components/communities/community-amenities.tsx</files>
  <read_first>
    - src/types/community.ts
    - src/components/motion/fade-in.tsx
    - src/components/motion/stagger-children.tsx
    - src/components/ui/button.tsx
    - src/components/ui/badge.tsx
    - src/app/globals.css
    - .planning/phases/03-schell-brothers-communities/03-UI-SPEC.md
  </read_first>
  <action>
Create three components following the UI-SPEC contracts exactly:

**1. `src/components/communities/community-hero.tsx`** (client component for video + animations):

```
"use client";
```

Props: `{ community: Community; onScheduleTour: () => void }`

Structure per UI-SPEC `CommunityHero`:
- Full-bleed section, min-height: `min-h-[70vh] lg:min-h-[70vh]` (mobile 50vh: `min-h-[50vh]`)
- Background: `next/image` with `priority`, `fill`, `object-cover`, blur placeholder using `featured_image_url`. If `is_sold_out`, add CSS `filter: grayscale(0.6) brightness(0.8)`
- Gradient overlay: `div` with `absolute inset-0` and `background: linear-gradient(transparent 30%, rgba(10,10,10,0.85) 100%)`
- Content overlay at bottom-left: `absolute bottom-0 left-0 p-8 lg:p-12 pb-12 lg:pb-16`
  - Location label: `{city}, {state}` — 12px uppercase muted, letter-spacing 0.05em, font-body (Montserrat)
  - Community name: 48px desktop / 32px mobile, font-display (Playfair), font-bold, text-foreground, text-shadow `0 2px 8px rgba(0,0,0,0.6)`
  - Price: `From $XXX,XXX` — 28px desktop, font-display, font-bold, text-accent (gold), tabular-nums. Format `price_from` with `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 })`. Skip if `price_from` is null.
  - CTA Button: `triggerLabel` = "Schedule a Tour" for normal, "Join Waitlist" for sold-out. Primary variant (gold bg). Height 44px. `onClick={onScheduleTour}`
  - Sold-out badge: top-right corner, destructive variant badge, text "SOLD OUT" (12px uppercase bold)
- Framer Motion stagger: FadeIn each element with 100ms stagger (location 0ms, name 100ms, price 200ms, CTA 300ms), translate-y 16px -> 0, duration 600ms

**2. `src/components/communities/community-overview.tsx`** (client component for expand/collapse):

```
"use client";
```

Props: `{ community: Community }`

Structure per UI-SPEC `CommunityOverview`:
- Section within 1280px max-width container. Body text max-width 800px.
- Heading: "About {community.name}" — Heading 28px, font-display, font-bold
- Description: `community.description` — Body 16px, font-body, line-height 1.6, text-foreground
- If description exceeds ~500 chars (proxy for 6 lines), truncate with a fade gradient and "Read more" / "Show less" toggle link (text-accent, cursor-pointer)
- Use `useState` for expanded/collapsed state
- Sales center variant: if `community.sales_center_address` is truthy, render a small `bg-card border border-border rounded-lg p-4` card below description with MapPin icon + address + "Get Directions" ghost button linking to `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(address)}`
- Wrap in FadeIn with viewport trigger

**3. `src/components/communities/community-amenities.tsx`** (server component):

Props: `{ amenities: { name: string; icon?: string }[] }`

Structure per UI-SPEC `CommunityAmenities`:
- If `amenities.length === 0`, return `null` (hidden entirely)
- Heading: "Community Amenities" — 28px font-display font-bold
- Grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4`
- Each amenity: `bg-card rounded-lg px-4 py-2 flex items-center gap-3 border border-border`
  - Lucide icon 16px in text-accent (gold). Icon map:
    - "Pool" | "Swimming" -> `Waves`
    - "Fitness" | "Gym" -> `Dumbbell`
    - "Playground" -> `Baby`
    - "Clubhouse" | "Club" -> `Building`
    - "Trail" | "Walking" | "Path" -> `TreePine`
    - "Tennis" -> `CircleDot`
    - "Dog" | "Pet" -> `Dog`
    - default -> `Check`
  - Match case-insensitively against amenity `name` (use `.toLowerCase().includes()`)
  - Amenity name: Body 16px, font-body, text-foreground
- Wrap grid items with StaggerChildren for viewport-triggered animation (30ms stagger per chip, scale 0.95->1 + opacity)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && test -f src/components/communities/community-hero.tsx && test -f src/components/communities/community-overview.tsx && test -f src/components/communities/community-amenities.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - community-hero.tsx contains `"use client"` directive
    - community-hero.tsx exports `CommunityHero` component
    - community-hero.tsx uses `next/image` with `priority` and `fill` props
    - community-hero.tsx renders gradient overlay `rgba(10,10,10,0.85)`
    - community-hero.tsx formats price with `Intl.NumberFormat`
    - community-hero.tsx shows "SOLD OUT" badge when `is_sold_out` is true
    - community-hero.tsx CTA text is "Join Waitlist" when sold out, "Schedule a Tour" otherwise
    - community-overview.tsx contains `"use client"` directive
    - community-overview.tsx exports `CommunityOverview` component
    - community-overview.tsx renders "About {name}" heading
    - community-overview.tsx has expand/collapse logic with "Read more" / "Show less"
    - community-overview.tsx renders sales center card with Google Maps directions link
    - community-amenities.tsx exports `CommunityAmenities` component
    - community-amenities.tsx returns null when amenities array is empty
    - community-amenities.tsx has icon map with Waves, Dumbbell, Baby, Building, TreePine, CircleDot, Dog, Check
    - community-amenities.tsx renders 4-column grid on desktop
    - All three files pass TypeScript compilation (`npx tsc --noEmit`)
  </acceptance_criteria>
  <done>CommunityHero (video/image + gradient + CTA), CommunityOverview (description + read-more + sales center), CommunityAmenities (icon grid) all implemented per UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: CommunitySchools + CommunityHoa + CommunityFloorPlans</name>
  <files>src/components/communities/community-schools.tsx, src/components/communities/community-hoa.tsx, src/components/communities/community-floor-plans.tsx</files>
  <read_first>
    - src/types/community.ts
    - src/types/listing.ts
    - src/components/communities/community-hero.tsx
    - src/components/ui/badge.tsx
    - src/components/motion/fade-in.tsx
    - .planning/phases/03-schell-brothers-communities/03-UI-SPEC.md
  </read_first>
  <action>
Create three components following UI-SPEC contracts:

**1. `src/components/communities/community-schools.tsx`** (server component):

Props: `{ community: Community }`

Structure per UI-SPEC `CommunitySchools`:
- If all three school fields (`school_elementary`, `school_middle`, `school_high`) are null, return `null`
- Heading: "Schools" — 28px font-display font-bold
- Subheading: `{school_district} School District` — Body 16px, text-muted-foreground. Only if `school_district` is truthy.
- Three cards in a row (`grid grid-cols-1 md:grid-cols-3 gap-4`), only rendered if the field is non-null:
  - Each card: `<article>` element, `aria-label="{level}: {school name}"`, bg-card, 1px border-border, rounded-lg, p-6, `border-l-[3px] border-l-green-500` (success color, left accent)
  - School level label: "Elementary School" / "Middle School" / "High School" — Label 12px uppercase muted, letter-spacing 0.05em
  - School name: Body 16px weight 700, text-foreground
- Wrap in FadeIn

**2. `src/components/communities/community-hoa.tsx`** (server component):

Props: `{ community: Community }`

Structure per UI-SPEC `CommunityHoa`:
- If all HOA fields (`hoa_name`, `hoa_fee`, `hoa_yearly_fee`) are null, return `null`
- Heading: "HOA Information" — 28px font-display font-bold
- Info card: bg-card, 1px border-border, rounded-lg, p-6
- Three-column layout (`grid grid-cols-1 md:grid-cols-3 gap-4`):
  - Column 1: HOA Name label (12px uppercase muted) + value (16px bold). Show `hoa_name` or skip column if null.
  - Column 2: "Monthly Fee" label + `$${hoa_fee}/mo` (16px bold, tabular-nums). Skip if null.
  - Column 3: "Annual Fee" label + `$${hoa_yearly_fee}/yr` (16px bold, tabular-nums). Skip if null.
- If all fee values are null but `hoa_name` exists, show just the name.
- No-HOA variant: if `hoa_fee === 0 && hoa_yearly_fee === 0`, display "No HOA fees for this community" in muted-foreground.
- Wrap in FadeIn

**3. `src/components/communities/community-floor-plans.tsx`** (client component for hover effects):

```
"use client";
```

Props: `{ listings: Listing[]; communityName: string }`

Structure per UI-SPEC `CommunityFloorPlans`:
- Heading: "Available Floor Plans" — 28px font-display font-bold. Next to heading, a badge: `{listings.length} plans` (Label 12px, bg-accent/20 text-accent)
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Each floor plan card (derived from `Listing` data):
  - Container: bg-card, 1px border-border, rounded-lg, overflow-hidden, cursor-pointer
  - Top: `next/image` with aspect-ratio 4:3 using `raw_data.featured_image_url` or `photos[0]` from listing. Use `loading="lazy"`. If no image, use a placeholder gradient div.
  - Bottom (p-4):
    - Floor plan name: `raw_data.floorPlanName` or listing `address_full` — Body 16px weight 700
    - Price: `list_price` formatted as USD — Body 16px weight 700 text-accent tabular-nums
    - Specs: `{bedrooms} Beds | {bathrooms} Baths | {area} Sqft` — Label 12px muted uppercase 0.05em letter-spacing. Use range format if raw_data has min/max values.
    - Filter tags: from `raw_data.filters` array, render as small badges (12px, bg-muted rounded-full px-2 py-0.5). Max 3 tags visible.
  - Hover: `hover:border-border-hover hover:-translate-y-0.5 transition-all duration-200`
  - Click: opens `https://www.schellbrothers.com${raw_data.floorPlanUrl || heartbeat_page_url}` in new tab (`target="_blank"` `rel="noopener"`)
  - External link icon indicator: small ExternalLink icon (12px) in top-right of card
- Empty state: "No floor plans currently available. Check back soon or schedule a tour for the latest options." — Body 16px text-muted-foreground
- Wrap card grid in StaggerChildren (60ms stagger per card)

Access `raw_data` from listing: the listing's `raw_data` JSONB column contains keys `floorPlanName`, `communityId`, `communityName`, `communityUrl`, `featured_image_url`, `filters`. Cast to access: `(listing as any).raw_data` or create a helper type.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && test -f src/components/communities/community-schools.tsx && test -f src/components/communities/community-hoa.tsx && test -f src/components/communities/community-floor-plans.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - community-schools.tsx exports `CommunitySchools` component
    - community-schools.tsx returns null when all school fields are null
    - community-schools.tsx renders `<article>` elements with aria-label
    - community-schools.tsx has green left border accent (`border-l-green-500` or `border-l-[var(--success)]`)
    - community-schools.tsx renders school level labels "Elementary School", "Middle School", "High School"
    - community-hoa.tsx exports `CommunityHoa` component
    - community-hoa.tsx returns null when all HOA fields are null
    - community-hoa.tsx renders 3-column grid with HOA name, monthly fee, annual fee
    - community-hoa.tsx shows "No HOA fees for this community" when fees are zero
    - community-floor-plans.tsx contains `"use client"` directive
    - community-floor-plans.tsx exports `CommunityFloorPlans` component
    - community-floor-plans.tsx renders count badge with plans count
    - community-floor-plans.tsx links to schellbrothers.com with `target="_blank"` and `rel="noopener"`
    - community-floor-plans.tsx renders empty state message when listings array is empty
    - All files pass TypeScript compilation
  </acceptance_criteria>
  <done>CommunitySchools, CommunityHoa, CommunityFloorPlans all implemented per UI-SPEC with proper empty states, accessibility, and animations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Heartbeat data -> component rendering | Community data from DB rendered in React JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Community description XSS | mitigate | React JSX renders text content as escaped strings by default; no dangerouslySetInnerHTML used anywhere in community components |
| T-03-08 | Tampering | Floor plan external link injection | mitigate | External links always prefixed with `https://www.schellbrothers.com`; rel="noopener" prevents opener access |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- All 6 component files exist in src/components/communities/
- Each component exports a named function matching its filename
</verification>

<success_criteria>
- All 6 community detail components render correctly with Community type data
- Empty states handled gracefully (return null or fallback message)
- Accessibility attributes present (aria-label on articles, decorative icons hidden)
- Animations use FadeIn/StaggerChildren from existing motion wrappers
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-schell-brothers-communities/03-03-SUMMARY.md`
</output>
